// Style reminder: home is the compact dark-glass launch surface with orange play action and realtime public stats.

import { ArrowRight, CircleDollarSign, Crown, Gamepad2, Loader2, Sparkles, Trophy, UsersRound } from "lucide-react";
import { Link } from "wouter";
import { usePlayer } from "@/contexts/PlayerContext";
import { walletValue } from "@/lib/format";
import StatPill from "@/components/player/StatPill";

export default function Home() {
  const { player, stats, loading, telegramAvailable } = usePlayer(); const wallet = walletValue(player?.play_wallet);
  return <div className="px-3 py-4"><section className="relative overflow-hidden rounded-3xl border border-white/[0.07] bg-[radial-gradient(circle_at_80%_0%,rgba(255,140,0,0.22),transparent_35%),linear-gradient(145deg,#1a1a2e,#111326)] p-5 shadow-[0_18px_45px_rgba(0,0,0,0.25)]"><div className="absolute -right-10 -top-10 h-36 w-36 rounded-full bg-[#FF8C00]/10 blur-2xl" /><div className="relative flex items-start justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[0.22em] text-[#FFB45C]">KelemBingo</p><h1 className="mt-2 max-w-[210px] text-2xl font-black leading-tight tracking-tight">Your next win is one card away.</h1><p className="mt-2 max-w-[230px] text-xs leading-relaxed text-white/45">Fast rounds, transparent pools, and live bingo from Telegram.</p></div><img src="/images/kelembingo-mark.webp" alt="Kelem Bingo mark" className="h-16 w-16 rounded-2xl object-contain drop-shadow-[0_8px_22px_rgba(255,140,0,0.38)]" /></div><Link href={player ? "/select" : "#play"} className="mt-5 flex items-center justify-between rounded-2xl bg-gradient-to-r from-[#FF8C00] to-[#FF6B00] px-4 py-3 text-sm font-black shadow-[0_10px_25px_rgba(255,140,0,0.22)] transition-transform active:scale-[0.97] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white"><span className="flex items-center gap-2"><Gamepad2 className="h-4 w-4" /> Play a round</span><ArrowRight className="h-4 w-4" /></Link></section>{!telegramAvailable && !loading && <div className="mt-3 flex items-start gap-3 rounded-2xl border border-[#F59E0B]/20 bg-[#F59E0B]/10 px-4 py-3 text-xs text-[#FCD34D]" role="status"><Sparkles className="mt-0.5 h-4 w-4 shrink-0" /><p>Open KelemBingo inside Telegram to authenticate and play with your real wallet.</p></div>}<section className="mt-4 grid grid-cols-2 gap-2.5" aria-label="Live game statistics"><StatPill label="Active cartelas" value={loading ? <Loader2 className="h-4 w-4 animate-spin" /> : (stats?.active_cartelas ?? 0).toLocaleString()} tone="green" /><StatPill label="Games played" value={loading ? <Loader2 className="h-4 w-4 animate-spin" /> : (stats?.games_played ?? 0).toLocaleString()} tone="blue" /><StatPill label="Winners today" value={loading ? <Loader2 className="h-4 w-4 animate-spin" /> : (stats?.winners_today ?? 0).toLocaleString()} tone="purple" /><StatPill label="Play wallet" value={`${wallet.toLocaleString()} ETB`} tone="orange" /></section><section className="mt-5"><div className="mb-3 flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-white/30">How it works</p><h2 className="mt-1 text-lg font-black">Simple, live, transparent.</h2></div><CircleDollarSign className="h-5 w-5 text-[#FF8C00]" /></div><div className="grid gap-2.5"><Feature icon={<UsersRound />} title="Choose your cartela" body="Pick available cards before the round timer closes." tone="blue" /><Feature icon={<Trophy />} title="Follow every call" body="The game board updates in realtime with the current number." tone="green" /><Feature icon={<Crown />} title="Claim your bingo" body="A valid server-verified claim settles the final payout." tone="orange" /></div></section></div>;
}
function Feature({ icon, title, body, tone }: { icon: React.ReactNode; title: string; body: string; tone: "blue" | "green" | "orange" }) { const iconColor = tone === "orange" ? "text-[#FF8C00] bg-[#FF8C00]/10" : tone === "green" ? "text-[#34D399] bg-[#10B981]/10" : "text-[#60A5FA] bg-[#3B82F6]/10"; return <div className="flex items-center gap-3 rounded-2xl border border-white/[0.06] bg-[#1A1A2E]/80 px-3.5 py-3"><div className={`flex h-9 w-9 shrink-0 items-center justify-center rounded-xl ${iconColor}`}>{icon}</div><div><h3 className="text-xs font-black">{title}</h3><p className="mt-1 text-[11px] leading-relaxed text-white/40">{body}</p></div></div>; }
