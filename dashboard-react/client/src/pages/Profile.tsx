// Style reminder: profile preserves the compact identity card, orange action emphasis, and honest estimated-earnings label.

import { LogOut, ShieldCheck, UserRound } from "lucide-react";
import { usePlayer } from "@/contexts/PlayerContext";
import { etb, walletValue } from "@/lib/format";

export default function Profile() { const { player, logout, telegramAvailable } = usePlayer(); const wins = player?.wins || 0; const games = player?.total_games || player?.games_played || 0; return <div className="px-3 py-4"><div className="mb-4 flex items-end justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-white/30">Player account</p><h1 className="mt-1 text-2xl font-black">Profile</h1></div><UserRound className="h-5 w-5 text-[#8B5CF6]" /></div><section className="rounded-3xl border border-white/[0.07] bg-[#1A1A2E]/80 p-5"><div className="flex items-center gap-3"><div className="flex h-14 w-14 items-center justify-center rounded-2xl bg-gradient-to-br from-[#FF8C00] to-[#DC2626] text-2xl font-black">{(player?.first_name || "P").charAt(0).toUpperCase()}</div><div><h2 className="text-lg font-black">{player?.first_name || "Telegram player"}</h2><p className="mt-1 text-xs text-white/35">{player?.username ? `@${player.username}` : "Identity ready inside Telegram"}</p></div></div><div className="mt-5 grid grid-cols-2 gap-2.5"><Metric label="Games" value={games} /><Metric label="Wins" value={wins} /><Metric label="Wallet" value={etb(walletValue(player?.play_wallet))} /><Metric label="Estimated earnings" value={etb(wins * 10 * 0.75)} /></div></section><div className="mt-4 flex items-start gap-3 rounded-2xl border border-[#3B82F6]/20 bg-[#3B82F6]/10 px-4 py-3 text-xs text-[#93C5FD]"><ShieldCheck className="mt-0.5 h-4 w-4 shrink-0" /><p>{telegramAvailable ? "Your account is authenticated through Telegram." : "Open the app from Telegram to access your authenticated account."}</p></div><button onClick={logout} className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl border border-red-500/25 bg-red-500/10 px-4 py-3 text-xs font-black text-red-300 transition-transform active:scale-[0.97] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-red-400"><LogOut className="h-4 w-4" /> Sign out locally</button></div>; }
function Metric({ label, value }: { label: string; value: React.ReactNode }) { return <div className="rounded-xl border border-white/[0.06] bg-white/[0.03] p-3"><p className="text-[9px] font-semibold uppercase tracking-[0.14em] text-white/30">{label}</p><p className="mt-1 text-sm font-black text-white/85">{value}</p></div>; }
